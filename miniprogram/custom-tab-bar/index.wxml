<view class="tab-bar">
  <view
    class="tab-item {{item.selected ? 'active' : ''}}"
    wx:for="{{list}}"
    wx:key="pagePath"
    data-path="{{item.pagePath}}"
    bindtap="switchTab"
  >
    <view class="tab-dot"></view>
    <text>{{item.text}}</text>
  </view>
</view>
